### 打印机队列

>n = input().split()
>
>printers = {i: [] for i in range(1, n)}

这个报错的原因非常直接：`n = input().split()` 这一行代码读入后，`n` 变成了一个**列表**（例如 `['7']`），而 `range()` 函数只能接受一个**整数**（如 `7`）。

1. **数据类型错误**：`n = input().split()` 会返回列表。应改为 `n = int(input())`。
2. **`range` 范围错误**：你的打印机编号是 1 到 5，`range(1, n)` 如果 `n` 是 5，只会循环到 4。应改为 `range(1, 6)`。
3. **函数调用混淆**：
   - `printers(p)` 是在尝试“调用”字典，而不是“访问”字典。字典访问应该用方括号：`printers[p]`。
   - `heapq.heappush(printers, ...)`：这里你应该传入的是特定打印机的列表，即 `printers[p]`。
4. **数据类型统一**：输入进来的 `p` 和 `num` 默认是字符串，必须用 `int()` 转换，否则无法进行数值比较或作为字典索引。



### 流浪地球

`sys.stdin.readline()` 读取到的内容末尾通常包含一个换行符 `\n`。虽然在调用 `int()` 进行转换时，Python 会自动帮你忽略掉这个换行符，但 `.strip()` 还有更强大的功能：

- **清理多余空白**：它不仅能去掉末尾的 `\n`，还能去掉行首或行尾可能存在的**空格**或**制表符 (`\t`)**。
- **安全性**：有些比赛平台或测试文件的输入格式不太标准（比如某一行末尾多了一个空格），`split()` 处理起来可能比较随意，但 `.strip()` 能确保你拿到的是“干净”的字符串。





> printers = {i: [] for i in range(1, n+1)}
>
> ....
>
> heapq.heappush(printers[p], (-num, order, file_id))

打印机队列这题要写[p]，是因为这是一个字典，字典里面存储的列表，而heappush只接受列表作为堆对象，将()元组作为一个对象压入列表中







而在本题中已经是一个列表了，直接作为对象就好

> pq = []  #发动机点火队列
>
> heapq.heappush(pq, (t, p))



### 数据序列比大小

错误写法

```python
A_line1 = line1.sort()
B_line2 = line2.sort()
```

sort是原地排序

```python
line1.sort()
line2.sort()
```

