## 个人总结

`eval(input())` 你可以把它理解为：**“直接把输入的这串文字变成它看起来像的那个 Python 数据对象。**



宽度降序是为了淘汰掉长度相等的情况

```python
books.sort(key=lambda x: (x[0], -x[1]))
```



getMaxLIS(widths) 就是计算在满足长度递增的前提下，有多少宽度也是严格递增的