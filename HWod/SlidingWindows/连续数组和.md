# 连续数组和



```python
for right in range(n):
    cur_sum += nums[right]
    while cur_sum >= x:
        count += n - right
        # 尝试缩短区间
        cur_sum -= nums[left]
        left += 1
        return count
```

注意是 while 不是 if ,因为要做多次判断的